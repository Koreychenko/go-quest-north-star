# С Новым 2026 годом! 🌲☃️

# North Star Quest — Новогодний детективный квест

Интерактивный детективный квест для детей 10–15 лет, реализованный в виде сети Telegram-ботов. Игроку предстоит расследовать пропажу Рождественской звезды, общаясь с персонажами, роли которых исполняет нейросеть Gemini AI.

## О квесте

В Гимназии №32 Санкт-Петербурга произошло ЧП: накануне праздника в Новой Голландии пропала копия исторической Рождественской звезды. Если её не найти, традиционная церемония будет сорвана.

Игроку предстоит:
* Общаться с четырьмя персонажами, у каждого из которых свой характер, знания и мотивы.
* Собирать улики и фотографии.
* Передавать информацию между персонажами, чтобы продвинуться по сюжету.
* Найти похитителя и вернуть звезду.

### Персонажи
* **Лиза (@Liza...)** — ответственная за праздник, в диком стрессе. Главный координатор игрока.
* **Маша (@Masha...)** — подруга Лизы, отвечала за экскурсию, во время которой пропала звезда.
* **Катя (@Katya...)** — знает много о происходящем в школе, может прислать важные фото.
* **Аня (@Anya...)** — младшая сестра Маши, ученица 5 класса, которая может быть замешана в истории.
* **Главный бот** — помогает в начале и завершает квест.

## Технологии

* **Язык:** Go (Golang)
* **LLM:** Google Gemini AI (через API)
* **Платформа:** Telegram Bot API
* **Движок:** [go-quest](https://github.com/Koreychenko/go-quest)

## Требования

* Установленный [Go](https://go.dev/dl/) версии 1.22 или выше.
* 5 Telegram-ботов (создаются через [@BotFather](https://t.me/BotFather)).
* API ключ Google Gemini (получить на [Google AI Studio](https://aistudio.google.com/app/apikey)).

## Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository_url>
   cd quest
   ```

2. **Настройте переменные окружения:**
   Скопируйте файл `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```
   Откройте `.env` и заполните следующие поля:
   * `LLM_API_KEY`: ваш API ключ Gemini.
   * `MAIN_BOT_TOKEN`: токен главного бота.
   * `LIZA_BOT_TOKEN`: токен бота Лизы.
   * `MASHA_BOT_TOKEN`: токен бота Маши.
   * `KATYA_BOT_TOKEN`: токен бота Кати.
   * `ANYA_BOT_TOKEN`: токен бота Ани.

3. **Настройте промпты:**
   В файлах папки `prompts/` встречаются плейсхолдеры вида `{REPLACE_ME:@Username}`. Для того чтобы боты могли корректно давать ссылки друг на друга в диалогах, необходимо заменить эти значения на актуальные @username ваших созданных ботов во всех файлах папки `prompts`:
   * `{REPLACE_ME:@MainTelegramBot}` — юзернейм главного бота.
   * `{REPLACE_ME:@MashaTelegramAccountBot}` — юзернейм бота Маши.
   * `{REPLACE_ME:@KatyaTelegramAccountBot}` — юзернейм бота Кати.
   * `{REPLACE_ME:@AnyaTelegramAccountBot}` — юзернейм бота Ани.

4. **Загрузите зависимости:**
   ```bash
   go mod download
   ```

5. **Запустите приложение:**
   ```bash
   go run cmd/main/main.go
   ```

### Запуск через Docker

Если у вас установлен Docker и Docker Compose, вы можете запустить приложение одной командой.

1. **Соберите и запустите контейнер:**
   ```bash
   docker compose build
   docker compose up -d
   ```

   Приложение будет запущено в фоновом режиме. Файл `.env` подхватится автоматически.

## Структура проекта

* `cmd/main/main.go` — точка входа, инициализация ботов и обработчиков.
* `config.yaml` — конфигурация логики квеста: связи между ботами, состояния (states) и цели (goals).
* `prompts/` — системные инструкции для Gemini AI для каждого персонажа и состояния.
* `photos/` — изображения, которые боты отправляют игроку в ходе расследования.
* `.env.example` — шаблон файла с секретами.

## Как это работает

Приложение запускает пять независимых Telegram-ботов одновременно. Состояние прохождения (достигнутые цели) синхронизируется между ними. Gemini AI получает системный промпт, историю переписки и текущие знания персонажа, отвечая игроку в заданном стиле и формате JSON, который обрабатывается движком для переключения состояний или отправки медиа-файлов.
